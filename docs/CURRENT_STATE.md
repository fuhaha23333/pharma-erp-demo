
# 项目当前状态基线（CURRENT_STATE）

## 1. 文档信息

| 项目          | 内容                                                                                                                                                                           |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 文档版本      | v2.0                                                                                                                                                                           |
| 文档性质      | 动态事实基线                                                                                                                                                                   |
| 快照日期      | 2026-07-30（Asia/Shanghai）                                                                                                                                                    |
| 适用项目      | `pharma-erp-demo`                                                                                                                                                            |
| 当前分支      | `feat/database-baseline`                                                                                                                                                     |
| 本次收口开始时 HEAD | `ab8028e`                                                                                                                                                              |
| DB-BASE-01 Goal起始基线 | `9575856`                                                                                                                                                          |
| DB-BASE-01正式执行规格初版提交 | `679d1bc`                                                                                                                                                  |
| DB-BASE-01静态实施开始时HEAD | `d840c7e`                                                                                                                                                       |
| DB-BASE-01实施提交 | `ab8028e`                                                                                                                                                              |
| 主线冻结基线  | `cef15bc`；DOC-READY-01 开始时本地 `main`、`origin/main`、`origin/HEAD` 均指向该提交                                                                                   |
| 当前阶段      | 核心业务与数据库逻辑设计已经冻结；`DB-BASE-01`数据库基础、种子和隔离验证范围已完成；M1正在实施但尚未通过验收。                                                               |
| 本次 Goal     | `DB-BASE-01完成状态与仓库事实收口`                                                                                                                                          |
| 主要证据      | 当前 Git 状态和历史、实施提交`ab8028e`、用户提供的MySQL 8.4.10真实验证结果、冻结设计文档、三个DB-BASE-01实施文件、当前 Java 源码和测试                                    |

本文替代旧版 `CURRENT_STATE.md` 中基于 2026-07-18、`main`、`cad7ff6` 的历史快照结论。Git 历史继续保存旧快照，但旧结论不再代表当前仓库状态。

---

## 2. 文档用途与边界

### 2.1 用途

本文用于回答：

1. 当前仓库中实际存在什么；
2. 哪些设计已经冻结；
3. 哪些代码、SQL 和测试已经存在；
4. 哪些结果已经编译、自动化测试或手工验证；
5. 冻结设计与当前实现之间还差什么；
6. 下一 Goal 可以从哪里开始。

### 2.2 边界

本文是事实快照，不是需求、实施计划或合规结论。

本文不会把以下概念混为一谈：

```text
设计已经冻结
≠
数据库已经实现
≠
Java 业务已经实现
≠
系统已经通过合规验证
```

当前仓库仍是药品批发 ERP 验证型 Demo，不是正式商业 ERP，不表示已经通过 GSP 检查、计算机系统验证或药品监管验收，也不能直接承载真实药品经营数据。

---

## 3. 证据等级

| 证据等级         | 本文含义                                                                      |
| ---------------- | ----------------------------------------------------------------------------- |
| 设计已冻结       | 设计文档已完成并由 Git 提交或标签形成可追踪基线；不代表代码或数据库已经实现。 |
| 仓库文件存在     | 当前 Git 跟踪清单中存在该文件；不代表文件内容已运行。                         |
| 代码存在         | 当前源码中存在对应类、方法或配置；不代表接口、数据库或业务行为已经验证。      |
| 编译已验证       | 本次或明确记录的 Maven 编译命令成功；只证明当前源码能够完成该构建步骤。       |
| 自动化测试已验证 | 对应自动化测试实际运行并通过；结论只覆盖测试真正执行的行为。                  |
| 本地手工验证     | 用户在明确本地环境中手工执行并取得结果；不自动等于仓库内可重复验证。          |
| 仓库内可重复验证 | 仓库包含可再次执行的脚本、测试或命令入口，且其输入和断言边界明确。            |
| 未实现           | 在本次指定范围的源码、SQL、配置或前端中未发现对应实现。                       |
| 未验证           | 文件或代码可能存在，但没有取得运行、接口、数据库或业务验收证据。              |

同一对象可以同时具有多个等级。例如：Drug 代码和旧 SQL 均存在且后端编译通过，但 Drug 业务没有自动化测试、HTTP、数据库或浏览器验收证据。

---

## 4. Git 与冻结设计基线

### 4.1 DB-BASE-01实施时间线与当前收口Git事实

DB-BASE-01实施时间线：

- 当前分支：`feat/database-baseline`；
- DB-BASE-01 Goal起始基线：`9575856`；
- DB-BASE-01正式执行规格初版提交：`679d1bc`；
- DB-BASE-01静态实施开始时HEAD：`d840c7e`；
- DB-BASE-01实施提交：`ab8028e`；
- 当前仓库范围内适用的规则文件为根目录 `AGENTS.md`。

本次完成状态收口开始时的当前事实：

- 当前分支：`feat/database-baseline`；
- 本次收口开始时 HEAD：`ab8028e`；
- DB-BASE-01实施提交：`ab8028e`；
- 远程跟踪：`origin/feat/database-baseline`，已包含实施提交；
- 工作区：干净。

`cef15bc` 当前同时具有以下三个标签：

```text
business-state-machine-v1.1
role-permission-matrix-v1.1
database-design-demo-v1.0
```

### 4.2 已冻结文档

| 文档                               | 版本 | 状态     | 当前事实                                                 |
| ---------------------------------- | ---- | -------- | -------------------------------------------------------- |
| `docs/DEMO_SCOPE.md`             | v1.1 | 冻结基线 | 冻结全国核心范围、广东增强 Mock、里程碑和验收边界。      |
| `docs/BUSINESS_FLOW.md`          | v1.0 | 冻结基线 | 冻结 BF-01 至 BF-18 的业务流程和里程碑关系。             |
| `docs/BUSINESS_STATE_MACHINE.md` | v1.1 | 冻结基线 | 冻结 SM-01 至 SM-18 的状态、转换、守卫和审计语义。       |
| `docs/ROLE_PERMISSION_MATRIX.md` | v1.1 | 冻结基线 | 冻结 10 个演示角色、业务动作权限和不相容职责。           |
| `docs/DATABASE_DESIGN_DEMO.md`   | v1.0 | 冻结基线 | 冻结 51 张候选表、事务、约束、状态映射和分阶段实施方向。 |

后三份同步设计文档在 `cef15bc` 共同冻结。上述文档是目标设计依据，不是当前实现清单。

---

## 5. 当前项目阶段

当前阶段应准确表达为：

> 核心业务与数据库逻辑设计已经冻结；`DB-BASE-01`规定的六张系统权限表DDL、10/160/233种子和隔离验证已经完成；M1已经进入实施阶段，但Java数据库模型迁移和全国核心业务闭环尚未开始。

这意味着：

- 产品范围、业务流程、状态机、角色权限和数据库逻辑模型已经形成冻结基线；
- 51 张候选表中只有`DB-BASE-01`范围内的六张系统权限表形成正式仓库 DDL，其余45张仍未实施；
- 当前 Java 代码仍是旧数据库设计下的早期 Drug 原型；
- 系统权限数据库基础已经实现，但登录、Java权限拦截、角色分配Service、公共审计和全国核心业务闭环尚未实现；
- M2 广东增强和 M3 演示加固均未开始；
- M1当前为“进行中”，M2和M3为“未开始”；任何里程碑均未通过验收。

### 5.1 数据库实施与下一 Goal 准备状态

- 核心设计：已冻结；
- 事实基线：已更新；
- 实施计划：`docs/DEMO_PLAN.md` v1.0 / 执行基线已建立；
- Goal 模板：`docs/GOAL_TEMPLATE.md` v1.0 / 执行模板已建立；
- 旧 Drug 原型边界：已写入 `AGENTS.md`；
- `CURRENT_STATE`、`DEMO_PLAN`、`GOAL_TEMPLATE`和`DB-BASE-01`执行规格已经形成数据库实施前准备链；
- `DB-BASE-01`正式执行规格、三个实施文件和实施提交`ab8028e`已经形成；
- `DP-01`～`DP-03`已经在执行规格中关闭；
- 角色—权限映射语义已经收口：`QA-016`包含申请人粗粒度关系，关系总数为233，直接修改禁令和阶段性禁止边界已明确；
- `DB-BASE-01`已经在MySQL 8.4.10隔离Schema中完成真实验证，成功后测试Schema已删除并复查为0；
- `DB-BASE-01`状态为“已完成”；下一Goal `DB-MIG-01`为“准备中”，但正式规格尚未形成、`DP-04`尚未关闭，也未授权实现。

---

## 6. 当前仓库结构

本次完成状态收口开始时，当前Git跟踪39个文件，其中：

- 后端主 Java 文件：17 个；
- 后端测试 Java 文件：1 个；
- 全仓Markdown：14个，包含根目录`AGENTS.md`；
- docs/下Markdown：13个，包含`docs/goals/DB-BASE-01.md`；
- SQL文件：3个；
- scripts文件：1个；
- 前端文件：0 个。

当前主要结构：

```text
pharma-erp-demo/
├── AGENTS.md
├── backend/
│   ├── pom.xml
│   └── src/
│       ├── main/
│       │   ├── java/
│       │   │   └── com/fuhaha/pharmaerp/
│       │   │       ├── common/
│       │   │       ├── config/
│       │   │       └── modules/
│       │   │           ├── master/drug/
│       │   │           └── system/controller/
│       │   └── resources/application.yml
│       └── test/java/com/fuhaha/pharmaerp/PharmaErpApplicationTests.java
├── docs/
│   └── goals/
│       └── DB-BASE-01.md
├── frontend/                  本地目录存在，但没有Git跟踪文件
├── scripts/
│   └── verify_db_base_01.sh
└── sql/
    ├── 001_create_drug_table.sql
    └── db-base-01/
        ├── 001_create_system_permission_tables.sql
        └── 002_seed_system_permission_data.sql
```

当前仍为空的 Markdown 文件：

```text
docs/COMPLIANCE_MATRIX.md
docs/OPEN_QUESTIONS.md
docs/README.md
```

已建立的实施准备文档：

```text
docs/DEMO_PLAN.md v1.0 / 执行基线 已建立
docs/GOAL_TEMPLATE.md v1.0 / 执行模板 已建立
docs/goals/DB-BASE-01.md v1.0 / 执行规格 已建立
```

上述三个空文件不能作为已完成设计或已实现能力的证据。实施计划和 Goal 模板的建立也不代表新增业务能力。

---

## 7. 已完成的设计工作

### 7.1 已冻结的范围与里程碑

- `NATIONAL_DEFAULT`：全国药品批发核心流程；
- `GD_DEMO_PROFILE`：广东省级追溯与监管能力的 Mock 展示；
- M1：全国核心业务闭环；
- M2：广东增强与质量展示；
- M3：演示加固。

### 7.2 已冻结的业务设计

- BF-01 至 BF-18 的业务流程；
- SM-01 至 SM-18 的业务状态机；
- 系统管理员、采购员、收货员、验收员、质量管理员、质量负责人、仓库管理员、销售员、出库复核员、审计查看员共 10 个演示角色；
- 申请、审核、批准、执行和独立复核的职责分离；
- 后端权限阻断、状态历史、审计和不可无痕修改原则。

### 7.3 已冻结的数据库逻辑设计

数据库设计包含 51 张候选表：

```text
M1：36 张
M2：10 张
M3：5 张
```

设计已经冻结的重点包括：

- 系统权限、基础资料、首营资质、采购收货验收、批次库存、销售出库、质量事件、追溯 Mock、审计历史和演示运维数据域；
- `inventory_balance` 与 `inventory_ledger` 的库存模型；
- `inventory_lock` 与库存质量结论分离；
- `source_event_key` 入库幂等；
- `current_marker` 和 `active_marker` 唯一策略；
- 受控更正、不可变记录、事务边界和并发锁定规则。

这些均属于“设计已冻结”。当前只有六张系统权限表形成正式 DDL 并完成`DB-BASE-01`隔离验证，其余45张候选表和对应 Java 实现尚未形成。

---

## 8. 当前后端代码现状

### 8.1 技术栈

| 技术                    | 当前仓库事实                                            | 证据等级                               |
| ----------------------- | ------------------------------------------------------- | -------------------------------------- |
| Java                    | POM 指定 Java 17；DOC-STATE-02 运行环境为 Java 17.0.19  | 仓库文件存在；编译已验证               |
| Maven                   | 单 Maven 后端工程；DOC-STATE-02 运行环境为 Maven 3.9.16 | 仓库文件存在；编译已验证               |
| Spring Boot             | 3.3.5                                                   | 仓库文件存在；上下文测试已验证         |
| Spring Web / Validation | 已引入                                                  | 代码存在；编译已验证                   |
| MyBatis-Plus            | 3.5.9，含分页拦截器                                     | 代码存在；编译和上下文测试已验证       |
| MySQL                   | 运行时驱动已引入，默认配置指向`pharma_erp`            | DB-BASE-01已在MySQL 8.4.10隔离Schema验证；默认开发数据源未被本Goal修改 |
| Knife4j                 | 4.5.0，Springdoc/Knife4j 开关已配置                     | 配置存在；未验证 HTTP 文档端点         |
| Lombok                  | 已引入                                                  | 代码存在；编译已验证                   |

### 8.2 公共基础代码

当前存在：

- 统一响应 `Result<T>`；
- 分页响应 `PageResult<T>`；
- MyBatis-Plus MySQL 分页拦截器；
- `BizException`；
- `GlobalExceptionHandler`，覆盖业务异常、参数异常和兜底异常；
- `GET /health` 固定健康响应。

这些能力均已通过编译；当前没有 Controller/HTTP 自动化测试证明实际状态码、响应契约、分页 SQL或健康端点行为。

### 8.3 Drug 模块文件

当前 Drug 模块共有 10 个 Git 跟踪文件：

| 分层       | 文件或数量                                                 | 当前职责                                    |
| ---------- | ---------------------------------------------------------- | ------------------------------------------- |
| Controller | `DrugController`                                         | 新增、更新、详情、分页、启用、停用 6 个接口 |
| DTO        | `DrugCreateDTO`、`DrugUpdateDTO`、`DrugPageQueryDTO` | 请求参数和 Bean Validation                  |
| Entity     | `Drug`                                                   | 映射旧`drug` 表                           |
| Enum       | `DrugStatusEnum`                                         | `ENABLED` / `DISABLED`                  |
| Mapper     | `DrugMapper`                                             | 继承 MyBatis-Plus`BaseMapper<Drug>`       |
| Service    | `DrugService`、`DrugServiceImpl`                       | 旧 Drug 业务校验、查询、写入和状态切换      |
| VO         | `DrugVO`                                                 | Drug 展示字段                               |

当前接口：

```text
POST /master/drugs
PUT  /master/drugs/{id}
GET  /master/drugs/{id}
GET  /master/drugs/page
PUT  /master/drugs/{id}/enable
PUT  /master/drugs/{id}/disable
```

### 8.4 Drug 仍是旧模型

当前 Drug 实体和 SQL 仍具有以下旧设计特征：

- 状态只有 `ENABLED` 和 `DISABLED`；
- Entity 使用 `deleted` 和 `@TableLogic`；
- 旧字段使用 `drug_name`、`manufacturer` 等命名；
- 未包含冻结数据库设计要求的 `business_category_code`、效期控制、近效期阈值和追溯管理级别；
- 启用只检查旧核心字段完整，不校验首营品种、供应商或质量审批；
- 更新关键药品字段没有受控更正或审计链。

冻结设计中的 Drug 状态为：

```text
DRAFT
INCOMPLETE
READY_FOR_FIRST_APPROVAL
DISABLED
```

因此，当前 Drug 模块只能作为旧数据库设计下的早期原型，不能作为新模块的数据模型或实现范例。后续应依据冻结设计原地适配，而不是修改冻结设计迁就旧代码。

### 8.5 当前跟踪配置

仓库当前只跟踪 `backend/src/main/resources/application.yml`：

- 服务端口为 8080；
- 应用名为 `pharma-erp`；
- 默认数据源指向 `127.0.0.1:3306/pharma_erp`；
- 默认用户名为 `root`，提交配置中的密码为空；
- JDBC 参数仍使用 `serverTimezone=Asia/Shanghai`；
- MyBatis-Plus 全局配置仍启用 `deleted` 逻辑删除；
- Springdoc 和 Knife4j 开关已启用；
- 没有 Git 跟踪的 test/dev/prod Profile 配置。

以上只是当前仓库开发默认配置。冻结数据库设计要求应用按 UTC 写入并校验会话时区，当前配置尚未完成对应迁移。`DB-BASE-01`通过独立login-path和隔离Schema验证数据库基线，没有修改或验证默认`pharma_erp`开发数据源，也没有完成Profile隔离或HTTP文档端点验证。

---

## 9. 当前数据库与 SQL 现状

### 9.1 仓库 SQL

当前 `git ls-files 'sql/**'` 包含：

```text
sql/001_create_drug_table.sql
sql/db-base-01/001_create_system_permission_tables.sql
sql/db-base-01/002_seed_system_permission_data.sql
```

旧`sql/001_create_drug_table.sql`只创建旧版 `drug` 单表，包含：

- 自增 `BIGINT` 主键；
- `drug_code` 唯一键；
- `ENABLED` / `DISABLED` 状态；
- `deleted` 逻辑删除字段；
- InnoDB 和 `utf8mb4`。

`sql/db-base-01/001_create_system_permission_tables.sql`提供六张系统权限表正式DDL；`sql/db-base-01/002_seed_system_permission_data.sql`提供10个角色、160个权限和233条角色—权限关系。

### 9.2 DB-BASE-01完成状态与剩余边界

- `DB-BASE-01`已经完成；
- 仓库已存在`sys_user`、`sys_role`、`sys_permission`、`sys_role_assignment`、`sys_user_role`、`sys_role_permission`六张表的正式DDL；
- 仓库已存在10/160/233显式种子SQL；
- 仓库已存在`scripts/verify_db_base_01.sh`可重复隔离验证脚本；
- MySQL 8.4.10真实验证已经通过；
- 隔离测试Schema验证成功后已经删除，复查数量为0；
- 默认开发Schema `pharma_erp`未被本Goal修改；
- 旧Drug SQL仍是待迁移原型；
- 其余45张候选表仍未实施；
- `inventory_balance`、`inventory_ledger`、质量、销售、追溯和审计表尚不存在于 SQL；
- 51张候选表尚未全部转成仓库DDL，也尚未纳入长期迁移机制。

必须继续区分：

```text
六张系统权限表DDL文件存在并在隔离Schema验证通过
≠
默认开发数据库pharma_erp已经持久部署六张表
```

### 9.3 已完成实现与未实现能力

当前已经完成的数据库权限基础：

- 六张系统权限表DDL；
- 10个角色种子；
- 160个权限种子；
- 233条角色—权限关系；
- 数据库约束和可重复隔离验证脚本。

当前仍未实现登录、密码校验、Token或Sa-Token、Java权限拦截、角色分配Service、HTTP接口、前端和公共审计表。上述数据库基础不得表述为“权限系统已经完成”或“角色审批已经完成”。

旧 SQL 与冻结数据库设计冲突时：

```text
冻结文档 = 目标依据
当前源码和旧 SQL = 实现现状
```

不得把旧 `drug` 表直接扩展成全部新模型，也不得把旧单表 DDL 计入 51 张候选表已经实现。

---

## 10. 当前构建与测试状态

### 10.1 DB-BASE-01后端回归结果

| 命令                                           | DB-BASE-01结果                                              | 能证明什么                         | 不能证明什么                                          |
| ---------------------------------------------- | ----------------------------------------------------------- | ---------------------------------- | ----------------------------------------------------- |
| `mvn -f backend/pom.xml -DskipTests compile` | `BUILD SUCCESS`                                           | 当前旧后端在实施后仍能够编译。     | 不证明HTTP、Drug业务、登录或权限拦截可用。             |
| `mvn -f backend/pom.xml test`                | `BUILD SUCCESS`；`Tests run: 1`，失败、错误和跳过均为 0 | 当前唯一的 Spring 上下文测试通过。 | 不证明 Drug 业务、Java权限、业务流程或系统整体可用。  |

最终成功测试结果：

```text
Tests run: 1
Failures: 0
Errors: 0
Skipped: 0

测试类：
com.fuhaha.pharmaerp.PharmaErpApplicationTests
```

已记录运行版本：

```text
Java：17.0.19
Spring Boot：3.3.5
MyBatis-Plus 运行日志版本：3.5.9
Maven：3.9.16
```

### 10.2 自动化测试覆盖边界

当前仓库只有一个测试类和一个无业务断言的 `contextLoads()` 测试。

因此，当前准确结论是：

```text
当前后端能够编译；
当前唯一的 Spring 上下文测试通过；
DB-BASE-01另有独立Shell脚本完成数据库结构、种子和负向约束验证。
```

当前没有：

- Drug Controller、Service 或 Mapper 自动化测试；
- Java数据库集成测试；
- 登录、权限、审计测试；
- 首营、采购、收货、验收、库存、销售、出库或追溯测试；
- HTTP 接口、浏览器或前端联调证据。

---

## 11. 本地数据库环境验证状态

### 11.1 2026-07-28数据库机制手工验证

用户已于 2026-07-28 完成本地手工机制验证：

| 项目                 | 用户提供的本地验证结果 |
| -------------------- | ---------------------- |
| MySQL 客户端和服务端 | 8.4.10 Homebrew        |
| 存储引擎             | InnoDB                 |
| 字符集               | `utf8mb4`            |
| 排序规则             | `utf8mb4_0900_ai_ci` |
| 严格 SQL 模式        | 已启用                 |
| 事务隔离级别         | `REPEATABLE-READ`    |
| 新会话时区           | `+00:00`             |
| CHECK 约束           | 手工验证通过           |
| marker 唯一性        | 手工验证通过           |
| 两连接并发唯一性     | 手工验证通过           |

该结果是`DB-BASE-01`实施前的本地机制证据，不代表51张表已经创建。

### 11.2 DB-BASE-01真实隔离验证

用户提供并确认的2026-07-30真实验证结果：

| 项目 | 结果 |
| --- | --- |
| MySQL服务器版本 | 8.4.10 |
| 隔离Schema | `pharma_erp_db_base_01_test` |
| 六张表DDL | 执行成功 |
| 字段 | 77 |
| 唯一约束 | 8 |
| CHECK | 24 |
| 显式普通索引 | 10 |
| 外键 | 21 |
| 正式角色 | 10 |
| 权限 | 160 |
| 角色—权限关系 | 233 |
| 负向数据库测试 | 通过 |
| 测试支撑数据清理 | 通过 |
| Maven compile | `BUILD SUCCESS` |
| Maven test | 1个通过，0失败，0错误，0跳过 |
| 最终测试Schema清理 | 成功 |
| 清理后`INFORMATION_SCHEMA.SCHEMATA`数量 | 0 |

仓库中的`scripts/verify_db_base_01.sh`已经形成可重复验证入口。本次完成状态收口只记录既有验证事实，没有重新连接数据库或运行Maven。

验证目标只限隔离Schema；成功后测试Schema已经删除。没有证据表明、也不得推断默认开发Schema`pharma_erp`已经持久部署这六张表。

---

## 12. 设计与实现差距

| 领域       | 冻结设计目标                                 | 当前实现事实                                                              | 结论                             |
| ---------- | -------------------------------------------- | ------------------------------------------------------------------------- | -------------------------------- |
| 系统权限   | 用户、角色、权限、分配审批和后端阻断         | 六张系统权限表DDL及10/160/233种子已完成；尚无登录、Java授权阻断或角色分配Service | 数据库基础已完成；应用能力未实现，TD-009仍需重新核验 |
| 审计       | 状态历史、成功/失败/越权审计、受控更正       | 未发现审计 Entity、表、Service 或当前操作人                               | 未实现；对应 TD-010              |
| Drug       | 新 Drug 状态和字段；首营与关键字段受控变更   | 旧`ENABLED/DISABLED`、`deleted/@TableLogic` 模型                      | 尚未迁移；对应 TD-011、TD-012    |
| 首营与准入 | 供应商首营、品种首营、客户资质               | 无对应 Java 和 SQL                                                        | 未实现                           |
| 采购       | 采购订单必须经过有效准入                     | 无对应 Java 和 SQL                                                        | 未实现                           |
| 收货与验收 | 收货和逐批验收分离                           | 无对应 Java 和 SQL                                                        | 未实现                           |
| 批次库存   | 批号库存、库存流水、效期、质量状态和并发控制 | 无`inventory_balance`、`inventory_ledger` 或当前规则要求的正式表/代码 | 未实现                           |
| 销售与出库 | 客户动态校验、批次占用、独立出库复核         | 无对应 Java 和 SQL                                                        | 未实现                           |
| 批号追溯   | 供应商至客户全过程追溯                       | 无对应 Java 和 SQL                                                        | 未实现                           |
| 广东增强   | 追溯码本地核验、Mock 请求和回执              | 无 Profile、Rule Pack、Adapter 或 Mock 实现                               | 未实现                           |
| 质量事件   | 事件、冻结、调查、审批和处置                 | 无对应 Java 和 SQL                                                        | 未实现                           |
| 前端       | Vue 3 + Vite + Element Plus 演示工程         | `frontend/` 没有 Git 跟踪文件                                           | 未实现；对应 TD-017              |
| 备份恢复   | 备份计划、执行、隔离恢复和核对               | 无对应脚本、表、代码或恢复记录                                            | 未实现；对应 TD-018              |
| 数据库基线 | 51 张候选表分阶段正式落地                    | 六张系统权限表DDL、种子和隔离验证已完成；其余45张及长期迁移机制未实施     | `DB-BASE-01`已完成；整体数据库落地仍未完成，TD-013需重新核验 |
| 自动化验收 | 正常、阻断、权限、审计、数据库和浏览器证据   | DB-BASE-01数据库断言及一个Spring上下文测试通过；无HTTP或浏览器业务验收   | 数据库基础证据已建立；业务验收未实现，Drug测试缺口对应TD-003 |

---

## 13. 当前风险与限制

### 13.1 主要风险

1. **设计与实现差距仍然很大。** `DB-BASE-01`只完成系统权限数据库基础；其余业务实现仍停留在旧 Drug 原型或尚不存在，不能以设计或单个数据库Goal完成度推断系统完成度。
2. **旧 Drug 模型不能继续扩展为目标模型。** `ENABLED/DISABLED`、逻辑删除和旧字段不足以表达首营、效期、追溯和受控变更。
3. **长期数据库迁移机制和其余45张表尚未实施。** `DB-BASE-01`文件可独立重复验证，但尚未被纳入长期迁移、初始化和版本管理机制。
4. **登录、Java权限阻断和审计尚未实现。** 六张表及权限种子不能替代身份、审批链、职责守卫和审计Service。
5. **业务自动化测试证据极薄。** 当前除DB-BASE-01数据库验证外仍只有一个Spring上下文测试，不能保护Drug或后续业务实现。
6. **没有业务用户可操作的前端。** 当前不能通过浏览器验收业务闭环。
7. **不能宣称合规或系统可用。** DB-BASE-01数据库验收通过不等于业务闭环、登录权限、审计、浏览器验收或计算机系统验证通过。

### 13.2 文档风险

`docs/TECH_DEBT.md` 仍是 2026-07-18、`cad7ff6` 的旧技术债快照，其中部分事实已经过期。例如 TD-015 中“核心设计文档仍为空”的结论已被当前冻结设计取代。本文记录当前事实；技术债状态和措辞需要在独立 Goal 中同步，不得继续把旧条目全部当成当前结论。

当前仍为空的 `COMPLIANCE_MATRIX.md` 表明合规要求、设计、实现和测试证据尚未形成仓库内正式追踪矩阵。

---

## 14. 当前下一 Goal

当前下一 Goal：

```text
DB-MIG-01：数据库迁移、初始化与验证机制
```

当前状态：

```text
准备中
```

上游事实：

- `DB-BASE-01`已经完成；
- 实施提交为`ab8028e`；
- 六张系统权限表DDL、10/160/233种子和隔离验证脚本已经进入仓库；
- MySQL 8.4.10隔离验证通过，测试Schema已清理。

`DB-MIG-01`的目标是为后续小批量DDL建立迁移顺序、初始化、版本识别、失败停止和验证机制，并纳管已经验收的`DB-BASE-01`。

当前准入边界：

- `DB-MIG-01`正式任务说明尚未形成；
- `DP-04`尚未关闭；
- 尚未授权实现；
- 不得直接开始编码、生成迁移文件或修改数据库；
- 本文不决定是否使用Flyway、Liquibase或其他具体方案。

当前阶段限制：

- 不得一次生成全部 51 张表；
- 不得顺带重构 Drug 模块；
- 不得开始采购、库存、销售或追溯业务实现；
- 不得修改冻结设计文档；
- 不得把旧 Drug 表直接扩展成全部新模型。

只有正式规格形成、`DP-04`经人工关闭且文件、数据库、配置与测试范围明确授权后，`DB-MIG-01`才能开始实现。

---

## 15. CURRENT_STATE 更新规则

1. 本文是动态事实基线，不是永久不变的历史报告。
2. 每次会改变“当前实现、当前验证或当前阶段”的 Goal 完成后，应评估是否同步更新本文。
3. 更新时必须记录快照日期、工作分支、主线冻结基线或 Goal 起始基线、任务开始时工作区状态、证据来源和实际测试结果。
4. 必须继续区分设计冻结、文件存在、代码存在、编译、自动化测试、手工验证、仓库可重复验证、未实现和未验证。
5. 冻结文档中的目标表、状态、权限和流程，在 DDL、Java、测试和验收证据出现前不得写成已实现。
6. 用户提供的本地手工验证必须标识为本地手工证据；只有验证脚本和断言进入仓库后，才能升级为仓库内可重复验证。
7. Maven、数据库、HTTP 和浏览器验证必须记录真实命令、结果和覆盖边界；失败不得改写为成功。
8. 旧实现与冻结设计冲突时，以冻结设计作为目标、以源码和 SQL 作为现状，不能修改冻结文档迁就旧代码。
9. 已过期结论应直接修正或删除，不为“保留历史”长期留在动态基线中；历史由 Git 保存。
10. 本文不得把 Demo 描述为已合规、已通过 GSP、可商用或可承载真实药品经营数据。

---

**文档结束**
